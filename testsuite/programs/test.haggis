IF @#$(*@#$&W&*EOIRUFEOISDFJNLKSD><MBMVFLKDSF$(IORE)) THEN
	SEND x TO DISPLAY
ELSE
	# Do nothing
END IF

FOREACH x FROM [23, 34.5, "dsfsfd"] DO
	SEND x TO DISPLAY
	f(34,34.5,x=3)
END FOREACH

FOR x FROM 1 TO 3 DO
	SEND x TO DISPLAY
END FOR